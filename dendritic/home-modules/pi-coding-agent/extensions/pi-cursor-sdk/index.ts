// dist/index.js is built by the package's prepare script on `npm i` (pi-npm-i),
// so it is absent on a fresh clone until deps are installed.
export { default } from "../vendor/fitchmultz/pi-cursor-sdk/dist/index.js";